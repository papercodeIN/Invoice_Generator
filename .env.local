# Optional local overrides — blank template, no secrets.
# Fill a value to override .env (or to configure a fresh clone).
# Blank entries are ignored and never override .env.
SMTP_HOST=
SMTP_PORT=
SMTP_SECURE=
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
ORDER_NOTIFICATION_EMAIL=
PORT=
